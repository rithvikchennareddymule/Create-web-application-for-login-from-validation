CREATE TABLE `login` ( 
  `username` varchar(45) NOT NULL, 
  `password` varchar(45) DEFAULT NULL, 
  PRIMARY KEY (`username`) 
)  
 
 
 Insert SQL statement: 
INSERT INTO `mysql_database`.`login` (`username`, 
`password`) VALUES (“Rithvik”, “••••”);  
